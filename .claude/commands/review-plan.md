---
description: Stage 3 — fresh-context five-axis audit of plan.md
allowed-tools: Read, Edit, Glob, Grep
---

Invoke the `reviewer` subagent on `docs/plan.md`. The reviewer runs with a fresh context —
pass only specific concerns, NOT the planning rationale.

Specific concerns to flag (optional):

$ARGUMENTS

When the reviewer returns, show me the findings by axis and whether the plan is Ready for
Execution or Blocked.
