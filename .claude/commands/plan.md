---
description: Stage 2 — turn research.md into a phased implementation plan
allowed-tools: Read, Write, Edit, Glob, Grep
---

Invoke the `planner` subagent. It reads `docs/research.md` and produces `docs/plan.md`.

Additional constraints for this run (if any):

$ARGUMENTS

When the planner returns, show me its summary and recommend `/review-plan` next.
