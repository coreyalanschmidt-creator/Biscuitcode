# Tauri / Rust
target/
**/target/
*.rs.bk
Cargo.lock.bak

# Node / pnpm / Vite
node_modules/
.pnpm-store/
dist/
.vite/
*.tsbuildinfo

# Tauri-specific build outputs
src-tauri/gen/
src-tauri/target/
src-tauri/Cargo.lock

# Editor / OS
.vscode/
.idea/
*.swp
*~
.DS_Store
Thumbs.db

# Local config / secrets (paranoia — keys SHOULD live in the keyring, not on disk,
# but block accidental commits of any local override files just in case)
.env
.env.local
.env.*.local
*.pem
*.key
*.key.pub

# Release signing keys — stored in ~/.local/share/biscuitcode-release-keys/ by convention.
# These paths are listed here as an extra safety net in case anyone copies them into the repo.
biscuitcode-release.private.asc
biscuitcode-release.pub.asc
updater.key
updater.key.pub

# Test/coverage artifacts
coverage/
.nyc_output/
*.lcov

# BiscuitCode app-local caches (never end up in the repo)
.biscuitcode-cache/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS-generated junk
*.orig
*.rej
