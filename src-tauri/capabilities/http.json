{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "http",
  "description": "HTTP fetch allowlist. Anthropic API calls go via Rust (reqwest), not the webview fetch API. Phase 5 adds Anthropic to reqwest — no Tauri webview permission needed. Phase 6a will add OpenAI + Ollama the same way. See docs/design/CAPABILITIES.md.",
  "windows": ["main"],
  "permissions": []
}
