{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "shell",
  "description": "Shell access. Phase 1 baseline = empty (no commands). Each phase that needs shell-out (Phase 6a Ollama, Phase 7 LSP discovery, Phase 3 xdg-open) adds its specific commands here with strict argument validators. NEVER add a wildcard cmd. See docs/design/CAPABILITIES.md.",
  "windows": ["main"],
  "permissions": []
}
